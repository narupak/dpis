					CREATE TABLE PER_JETHRO(
					JETHRO_ID NUMBER(10) NOT NULL,	
					DEPARTMENT_ID NUMBER(10) NOT NULL,	
					MEETING_TIME VARCHAR2(4) NOT NULL,
					MEETING_YEAR VARCHAR2(4) NOT NULL,
					POS_NO VARCHAR2(10) NOT NULL,
					MEETING_DATE VARCHAR2(19) NULL,
					ORG_ID NUMBER(10) NULL,	
					ORG_NAME VARCHAR2(100) NULL,	
					PL_CODE VARCHAR2(10) NULL,
					PL_NAME VARCHAR2(100) NULL,
					PM_CODE VARCHAR2(10) NULL,
					PM_NAME VARCHAR2(100) NULL,
					CL_NAME VARCHAR2(50) NULL,
					NEW_ORG_ID NUMBER(10) NULL,	
					NEW_ORG_NAME VARCHAR2(100) NULL,	
					NEW_PL_CODE VARCHAR2(10) NULL,
					NEW_PL_NAME VARCHAR2(100) NULL,
					NEW_PM_CODE VARCHAR2(10) NULL,
					NEW_PM_NAME VARCHAR2(100) NULL,
					NEW_CL_NAME VARCHAR2(50) NULL,
					COMMITTEE_RESULT NUMBER(1) NULL,
					COMMITTEE_REMARK VARCHAR2(1000) NULL,
					OKP_MEETING_TIME VARCHAR2(4) NULL,
					OKP_MEETING_YEAR VARCHAR2(4) NULL,
					OKP_MEETING_DATE VARCHAR2(19) NULL,
					OKP_COMMITTEE_RESULT NUMBER(1) NULL,
					OKP_COMMITTEE_REMARK VARCHAR2(1000) NULL,
					UPDATE_USER NUMBER(11) NOT NULL,
					UPDATE_DATE VARCHAR2(19) NOT NULL,		
					CONSTRAINT PK_PER_JETHRO PRIMARY KEY (JETHRO_ID));
					CREATE INDEX IDX_PER_JETHRO ON PER_JETHRO (DEPARTMENT_ID, MEETING_TIME, MEETING_YEAR, POS_NO);

insert into per_co_level select * from d01011.per_co_level where cl_name not in (select cl_name from per_co_level)

insert into per_line (PL_CODE, PL_NAME, PL_SHORTNAME, PL_ACTIVE, UPDATE_USER, UPDATE_DATE, PL_TYPE, PL_SEQ_NO, LEVEL_NO_MIN, LEVEL_NO_MAX) 
select PL_CODE, PL_NAME, PL_SHORTNAME, PL_ACTIVE, UPDATE_USER, UPDATE_DATE, PL_TYPE, PL_SEQ_NO, LEVEL_NO_MIN, LEVEL_NO_MAX from d01011.per_line 
where pl_code not in (select pl_code from per_line)